# Cuentas en la nube (Supabase) — OPCIONAL.
# Copia este archivo a `.env` y rellena con los datos de tu proyecto Supabase.
# Si lo dejas vacío, el juego funciona en local (sin cuentas).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
