node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
.vite-dev.log
scripts/.cache
coverage

# Entorno local (claves Supabase)
.env
.env.local

# Fotos originales de la despedida: solo se suben los recortes ya hechos
# (public/despedida/oscar). El script las lee de aquí.
fotos-oscar/
